#include "main.h"
#include <stdio.h>
/**
 * _strlen - finds and outputs length of string.
 * @s: pointer to string.
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);
}
